# GPU Monitor Configuration
# Copy this file to ~/.gpu_monitor_config and customize

# Remote server hostname or IP address
GPU_SERVER_HOST=ganesha

# SSH username (leave empty to use current user)
# GPU_SERVER_USER=your_username

# Refresh interval in seconds (default: 300 = 5 minutes)
GPU_REFRESH_INTERVAL=300

# Show percentage numbers next to icon (e.g., "45%/73%")
# Set to true if you want to see utilization percentages in menubar
# Default: false (icon only)
GPU_SHOW_PERCENTAGES=false

# Enable debug logging (set to 1 to enable)
DEBUG_LOGGING=0
